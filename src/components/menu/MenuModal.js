import React from 'react';
import { Modal } from 'elements';
import { Portal, absolute } from 'utilities';

export default function MenuModal({ on, childen }) {
  return <Portal id={'menu'}>
    Portal
  </Portal>;
}
